<!--CUSTOM_HEADER_START//-->
[ATTR_CLASS]<div class="{ATTR_CLASS}"[ATTR_ID] id="{ATTR_ID}"[/ATTR_ID]>[/ATTR_CLASS][ATTR_CLASS_ELSE][ATTR_ID]<div id="{ATTR_ID}">[/ATTR_ID][/ATTR_CLASS_ELSE]

[TITLE]<h3>{TITLE}</h3>[/TITLE]
[SUBTITLE]<h4>{SUBTITLE}</h4>[/SUBTITLE]
[TEXT]{TEXT}[/TEXT]

<div class="custom-list" id="custom{ID}">
<!--CUSTOM_HEADER_END//-->

<!--CUSTOM_ENTRY_START//-->
    <div class="customEntry[ITEM_FIRST] first[/ITEM_FIRST][ITEM_LAST] last[/ITEM_LAST]" id="custom-item-{ITEM_INDEX}">
        [CUSTCTP_TITLE]<h5>{CUSTCTP_TITLE}</h5>[/CUSTCTP_TITLE]
        [CUSTCTP_TEXT]{CUSTCTP_TEXT}[/CUSTCTP_TEXT]
        [CUSTCTP_URL]<a href="{CUSTCTP_URL}">@@More@@ &raquo;</a>[/CUSTCTP_URL]
    </div>
<!--CUSTOM_ENTRY_END//-->

<!--CUSTOM_ENTRY_SPACER_START//--><!--CUSTOM_ENTRY_SPACER_END//-->

<!--CUSTOM_FOOTER_START//-->
</div>
[ATTR_CLASS]</div>[/ATTR_CLASS][ATTR_CLASS_ELSE][ATTR_ID]</div>[/ATTR_ID][/ATTR_CLASS_ELSE]
<!--CUSTOM_FOOTER_END//-->

<!--
    Custom Content Part template (rendered by include/inc_front/content/cnt60.article.inc.php)

    Loop meta tags per entry:
    {ITEM_INDEX}    0-based index of current element
    {ITEM_NUM}      1-based position
    {ITEM_TOTAL}    total number of elements
    [ITEM_FIRST]...[/ITEM_FIRST]           first element of the whole set
    [ITEM_LAST]...[/ITEM_LAST]             last element of the whole set
    [ITEM_FIRST_ELSE]...[/ITEM_FIRST_ELSE] all but the first element
    [ITEM_LAST_ELSE]...[/ITEM_LAST_ELSE]   all but the last element

    <!--ENTRY_DATA_START//--> sections inside CUSTOM_ENTRY are stripped from
    the rendered entry and joined into {DATA}, which is available in
    CUSTOM_HEADER/CUSTOM_FOOTER scope only (e.g. for carousel indicators).

    Custom fields of the assigned Custom Content Part type:
    {CUSTCTP_fieldname}                    field value
    [CUSTCTP_fieldname]...[/CUSTCTP_fieldname]  rendered only when field value not empty
    (also available without CUSTCTP_ prefix: {fieldname}, [fieldname]...[/fieldname])
    image/file fields resolve numeric file IDs to the frontend file URL

    Content part meta:
    {ID}, [TITLE]{TITLE}[/TITLE], [SUBTITLE]{SUBTITLE}[/SUBTITLE],
    [TEXT]{TEXT}[/TEXT], [ATTR_CLASS]/[ATTR_ID], [BEFORE]/[AFTER],
    {CPT_KEY}, {MODULE}, [CPT_TITLE], [CPT_DESC]
-->