# Rules
- Always use `php8` instead of `php` for all CLI commands in this project.
- Always use double quotes for HTML tag attributes.
- Always use single quotes for PHP strings unless variable interpolation is required.


